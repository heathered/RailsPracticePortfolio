<h1>Tag Index</h1>

<%= link_to("Add Tag", new_tag_path) %>

<% @tags.each do |tag| %>
  <p><%= link_to("#{tag.id}: #{tag.name}", tag_path(tag.id)) %></p>
<% end %>

<p><%= link_to("Home", root_path) %></p>


