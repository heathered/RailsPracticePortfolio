<h1>Contacts Index</h1>

<%= link_to("Add Contact", new_contact_path) %>

<% @contacts.each do |contact| %>
  <p><%= link_to("#{contact.id}: #{contact.name}", contact_path(contact.id)) %></p>
<% end %>

<p><%= link_to("Home", home_path) %></p>