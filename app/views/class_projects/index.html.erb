<h2>Projects</h2>

<% if current_user %>
  <%= link_to("Add Project", new_class_project_path) %>
<% end %>

<ul>
<% @projects.each do |project| %>
  <li><%= link_to("#{project.name}", class_project_path(project.id)) %></li>
<% end %>
</ul>