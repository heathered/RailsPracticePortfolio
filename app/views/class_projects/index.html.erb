<h1>Projects Index</h1>

<%= link_to("Add Project", new_project_path) %>

<% @projects.each do |project| %>
  <p><%= link_to("#{project.id}: #{project.name}", project_path(project.id)) %></p>
<% end %>